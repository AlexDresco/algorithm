import java.util.ArrayList;
import java.util.Collections;

public class Last {
    public static int testableMethod(ArrayList array) {
        int last = (int) array.get(array.size() - 1);
        return last;

    }
}
